fn transform (input : Nat) (input2 : [u8]) : [u8] {
    input
}

inductive Nat : Type {
    zero : Nat,
    succ : Nat -> Nat;                  // $(_ : Nat) -> Nat
    elim
}

//inductive List (a : Type) : Type {
//    nil : List a,
//    cons : a -> List a -> List a
//}